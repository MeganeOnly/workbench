# 运行日志
*.log

# 个人配置与本机数据（仓库提供 *.example.json 模板）
buttons.json
config.json

# 内部开发手册（含个人环境路径，仅本地使用）
DEV.md

# 临时调试脚本（AI 会话中的一次性产物，随用随删随建）
.check-*.ps1
.test-*.ps1

# 运行时状态（服务端自动生成/改写，含个人数据，不随仓库分发）
bookmarks.json
dida-state.json
push-state.json
feeds.json
syscards-state.json

# 自动生成的缓存与图标（换机后随「添加快捷方式」重新生成）
favicons/
public/icons/

# 第三方设计参考素材（来源见各目录 SOURCE.txt，不随仓库分发）
design-references/

# 设计草稿（评审中或已落地的方案文档，未稳定）
.plans/

# Office / LibreOffice 临时锁文件（编辑 buttons.json 等时产生）
.~lock.*#

# 凭证与隐私文件（防御性忽略）
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*credentials*
*cred*
*secrets*
*token*
*api*key*

# 编辑器与系统临时
.idea/
.vscode/settings.json
*~

# 本机数据 dump
*.dump

# 杂项文档与编辑器临时
*NOTES.md
*NOTES*.md
*-notes.md
*-notes*.md
LEGACY-LOCATION*.md
*legacy-location*.md

# 编辑器 / 备份 / 临时文件
*.tmp
*.bak
*.swp

